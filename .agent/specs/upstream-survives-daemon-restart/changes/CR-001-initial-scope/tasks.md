# CR-001 Tasks — upstream-survives-daemon-restart

**Status:** Stub — tasks to be populated by `/nvmd-tasks` after `/nvmd-clarify` + `/nvmd-plan`.

This file is an atomic precondition of CREATE per the spec state machine. The concrete
task list will be generated by the SpecKit pipeline; this placeholder reserves the slot.

## Expected task areas (pre-clarify sketch, non-authoritative)

- T1: Unix upstream detachment (setpgid at spawn, signal isolation)
- T2: Windows Job Object per-upstream ownership model
- T3: Handoff protocol v1 schema + serialization
- T4: `handoff_control.sock` / named pipe listener on old daemon
- T5: `handoff_control` dialer on new daemon
- T6: FD transfer via SCM_RIGHTS (Unix)
- T7: Handle transfer via DuplicateHandle (Windows)
- T8: Token handshake reuse (FR-11) for handoff authentication
- T9: Process.Detach() returning FDs without SIGTERM
- T10: Owner.Shutdown() handoff mode (release without close)
- T11: HandleGracefulRestart handoff orchestration
- T12: loadSnapshot reattachment by PID (Unix `/proc`, macOS equivalent, Windows OpenProcess)
- T13: Supervisor classification of termination cause (FR-4)
- T14: Reaper soft-close (30s drain, stdin-close before SIGTERM)
- T15: Daemon-crash survival test (SIGKILL + reattach)
- T16: Integration tests across Linux + macOS + Windows
- T17: Performance bench — NFR-1 (< 2s for 15 upstreams)
- T18: Documentation — README section on handoff semantics, constitution #9 verification

Actual task IDs, dependencies, [P]arallel markers, and acceptance criteria populated by
`/nvmd-tasks`.
